# 趋势监控重点解读列语义验收

状态：用户已批准语义；执行证据待补

## 验收需求

- `REQ-DOW-MONITOR-KEY-INTERPRETATION-COLUMN-001`

## 已批准语义

- 保留全部现有原始指标列；
- 在“日内走势”之后、“趋势 / 位置”之前新增约 260px 宽的“重点解读”列；
- 每只股票固定显示状态、证据、风险三行；
- 每行最多两个短语；
- 摘要只使用现有派生展示值与 10 秒突发异动状态；
- 缺失和延迟必须显示“数据不足”“待确认”或时效问题，不得补零；
- 只有异动和明确风险短语可使用红色，不给整格或整行染色；
- 红色不是唯一提示，异动必须保留可见文字和完整可访问名称。

## 正式信号边界

重点解读只回答当前盘面证据，不得输出“建议买入”“建议卖出”“立即操作”“止盈”
或“止损”，不得生成、清除、翻转或升级正式买卖信号。

本轮不得修改后端、19912 道氏引擎、WebSocket 订阅、轮询、通知、排序、持久化或
自动交易。买卖信号列继续作为唯一正式信号展示。

## 执行证据

原型审批、测试先行的 RED/GREEN、构建、规格契约、独立需求复核和发布边界证据在
实现阶段逐项补充。截图或原型不能替代摘要语义的可执行行为测试。
