# Issue #16 计划 v1

## 目标

输出 `GET /api/research/macd-stages`，返回逐标的逐日 MACD(10/20/7) 阶段序列。

## 初版方案

1. 在指标管线增加 MACD 10/20/7 列，沿用现有 EMA 写法。
2. 新增 `macd_stages.py`：按标的取全历史日线，向量化派生阶段（DIF 与 DEA 的相对位置、柱值增减），每行打状态标签。
3. 端点返回阶段行；每行附 `generation` 与当日 `raw` 行情字段。
4. T+1 由调用方拿到结果后自行推迟一天使用。
5. OOS 留待后续迭代。

## 交付物

- 指标列、阶段服务、研究端点、定向测试。

> 本方案仅为评审输入，不能作为实现依据。
