# 构建 Docker 镜像时排除的文件 / 目录。
# 与 .gitignore 思路一致: 只把 Dockerfile 真正需要的送进构建上下文,
# 减小上下文体积、加速上传、避免缓存误伤。
# 注意: .dockerignore 不识别 .gitignore 的取反(!)语法, 这里按白名单思路直接列举排除项。

# ===== 版本控制 / CI =====
.git
.github
.gitignore

# ===== 用户运行时数据(绝不应进镜像) =====
data
backend/data

# ===== Python 缓存 / 虚拟环境 =====
**/__pycache__
**/*.py[cod]
**/.pytest_cache
**/.mypy_cache
**/.ruff_cache
**/.venv
**/venv

# ===== IDE / 系统 =====
.idea
.vscode
.DS_Store
**/*.swp

# ===== 文档(镜像不需要; README.md 除外, Dockerfile 显式 COPY 了它) =====
docs

# ===== 桌面客户端打包(PyInstaller 专用, 与 Docker 部署无关) =====
packaging
backend/dist
backend/build

# ===== 开发脚本(本地启服务用, 镜像不用) =====
dev.sh
dev.ps1
scripts

# ===== 密钥(防止误打进公开镜像) =====
.env
.env.*
backend/.env

# ===== 其他构建产物 =====
**/node_modules
*.log
